apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "pg.fullname" . }}
  labels:
    {{- include "pg.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
  {{- with .Values.postgresql.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  serviceName: {{ include "pg.fullname" . }}-headless
  replicas: {{ add .Values.postgresql.replicaCount 1 }}
  {{- with .Values.postgresql.updateStrategy }}
  updateStrategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "pg.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: postgresql
  template:
    metadata:
      labels:
        {{- include "pg.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: postgresql
{{- if or .Values.postgresql.configuration .Values.postgresql.podAnnotations }}
      annotations:
{{- if .Values.postgresql.configuration }}
        checksum/postgresql-config: {{ include (print $.Template.BasePath "/postgresql-configmap.yaml") . | sha256sum }}
{{- end }}
      {{- with .Values.postgresql.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
    spec:
      {{- if .Values.repmgr.enabled }}
      terminationGracePeriodSeconds: {{ .Values.repmgr.terminationGracePeriodSeconds | default 120 }}
      {{- end }}
      securityContext:
        fsGroup: 103
      {{- if .Values.repmgr.enabled }}
      shareProcessNamespace: true
      serviceAccountName: {{ include "pg.fullname" . }}-repmgr
      {{- end }}
      affinity:
        {{- if .Values.postgresql.affinity }}
        {{- toYaml .Values.postgresql.affinity | nindent 8 }}
        {{- else }}
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  {{- include "pg.selectorLabels" . | nindent 18 }}
                  app.kubernetes.io/component: postgresql
              topologyKey: kubernetes.io/hostname
        {{- end }}
      initContainers:
        - name: fix-permissions
          image: busybox:1.37
          command: ['sh', '-c', 'chown -R 101:103 /var/lib/postgresql/data && if [ -d /var/lib/postgresql/data/pgdata ]; then chmod 0700 /var/lib/postgresql/data/pgdata; fi']
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
{{- if and .Values.postgresql.extensions.enabled .Values.repmgr.enabled }}
        - name: copy-base-ext
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ['sh', '-c', 'cp /usr/lib/postgresql/18/lib/*.so /ext-lib/ && cp /usr/share/postgresql/18/extension/* /ext-share/']
          volumeMounts:
            - name: ext-lib
              mountPath: /ext-lib
            - name: ext-share
              mountPath: /ext-share
{{- end }}
{{- if .Values.postgresql.extensions.enabled }}
        - name: copy-ext
          image: "{{ .Values.postgresql.image.repository }}:{{ .Values.postgresql.image.tag }}"
          imagePullPolicy: {{ .Values.postgresql.image.pullPolicy }}
          command: ['sh', '-c', 'cp /usr/lib/postgresql/18/lib/*.so /ext-lib/ && cp /usr/share/postgresql/18/extension/* /ext-share/']
          volumeMounts:
            - name: ext-lib
              mountPath: /ext-lib
            - name: ext-share
              mountPath: /ext-share
{{- end }}
{{- if .Values.repmgr.enabled }}
        - name: repmgr-init
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ["/usr/local/bin/entrypoint.sh", "init"]
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretUsernameKey" . }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretPasswordKey" . }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretDatabaseKey" . }}
            - name: REPMGR_USER
              value: {{ .Values.repmgr.username }}
            - name: REPMGR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretRepmgrPasswordKey" . }}
            - name: REPMGR_DB
              value: {{ .Values.repmgr.database }}
            - name: HEADLESS_SERVICE
              value: {{ include "pg.fullname" . }}-headless.{{ .Release.Namespace }}.svc.cluster.local
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: repmgr-config
              mountPath: /etc/repmgr
{{- end }}
{{- if .Values.postgresql.configuration }}
        - name: setup-config
          image: busybox:1.37
          command:
            - sh
            - -c
            - |
              CONF=/var/lib/postgresql/data/pgdata/postgresql.conf
              if [ -f "$CONF" ] && ! grep -q "include_dir = '/etc/postgresql/conf.d'" "$CONF"; then
                echo "include_dir = '/etc/postgresql/conf.d'" >> "$CONF"
              fi
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
{{- end }}
      containers:
        - name: postgresql
{{- if .Values.repmgr.enabled }}
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ["/usr/local/bin/entrypoint.sh", "postgres"]
{{- else }}
          image: "{{ .Values.postgresql.image.repository }}:{{ .Values.postgresql.image.tag }}"
          imagePullPolicy: {{ .Values.postgresql.image.pullPolicy }}
{{- end }}
          ports:
            - name: postgresql
              containerPort: 5432
              protocol: TCP
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretUsernameKey" . }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretPasswordKey" . }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretDatabaseKey" . }}
{{- if .Values.repmgr.enabled }}
            - name: REPMGR_USER
              value: {{ .Values.repmgr.username }}
            - name: REPMGR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretRepmgrPasswordKey" . }}
            - name: REPMGR_DB
              value: {{ .Values.repmgr.database }}
            - name: HEADLESS_SERVICE
              value: {{ include "pg.fullname" . }}-headless.{{ .Release.Namespace }}.svc.cluster.local
{{- end }}
{{- if .Values.pgbackrest.enabled }}
            - name: PGBACKREST_ENABLED
              value: "true"
            - name: PGBACKREST_STANZA
              value: {{ .Values.pgbackrest.stanza }}
            - name: PGBACKREST_REPO1_S3_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ required "pgbackrest.existingSecret.name is required" .Values.pgbackrest.existingSecret.name }}
                  key: {{ .Values.pgbackrest.existingSecret.accessKeyIdKey }}
            - name: PGBACKREST_REPO1_S3_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.pgbackrest.existingSecret.name }}
                  key: {{ .Values.pgbackrest.existingSecret.secretAccessKeyKey }}
{{- end }}
{{- if not .Values.repmgr.enabled }}
          lifecycle:
            postStart:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - |
                    for i in {1..30}; do
                      if pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" > /dev/null 2>&1; then
                        {{- if .Values.postgresql.configuration }}
                        CONF="$PGDATA/postgresql.conf"
                        if [ -f "$CONF" ] && ! grep -q "include_dir = '/etc/postgresql/conf.d'" "$CONF"; then
                          echo "include_dir = '/etc/postgresql/conf.d'" >> "$CONF"
                        fi
                        {{- end }}
                        HBA_FILE=$(psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -t -c "SHOW hba_file" | xargs)
                        if ! grep -q "host all all 10.0.0.0/8 trust" "$HBA_FILE"; then
                          sed -i '/host all all all scram-sha-256/i host all all 10.0.0.0/8 trust' "$HBA_FILE"
                        fi
                        {{- range .Values.postgresql.pgHba }}
                        if ! grep -qF '{{ . }}' "$HBA_FILE"; then
                          sed -i '/host all all all scram-sha-256/i {{ . }}' "$HBA_FILE"
                        fi
                        {{- end }}
                        psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SELECT pg_reload_conf();" > /dev/null 2>&1
                        {{- if .Values.postgresql.lifecycle.postStart.additionalCommands }}
                        {{ .Values.postgresql.lifecycle.postStart.additionalCommands | nindent 24 }}
                        {{- end }}
                        break
                      fi
                      sleep 1
                    done
{{- else }}
          lifecycle:
{{- include "pg.preStop" . | nindent 12 }}
{{- if or .Values.postgresql.configuration .Values.postgresql.pgHba .Values.postgresql.lifecycle.postStart.additionalCommands }}
            postStart:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - |
                    for i in {1..90}; do
                      if pg_isready -U "$POSTGRES_USER" -d "$POSTGRES_DB" > /dev/null 2>&1; then
                        {{- if .Values.postgresql.configuration }}
                        CONF="$PGDATA/postgresql.conf"
                        if [ -f "$CONF" ] && ! grep -q "include_dir = '/etc/postgresql/conf.d'" "$CONF"; then
                          echo "include_dir = '/etc/postgresql/conf.d'" >> "$CONF"
                        fi
                        {{- end }}
                        {{- if .Values.postgresql.pgHba }}
                        HBA_FILE=$(psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -t -c "SHOW hba_file" | xargs)
                        {{- range .Values.postgresql.pgHba }}
                        if ! grep -qF '{{ . }}' "$HBA_FILE"; then
                          sed -i '$ a {{ . }}' "$HBA_FILE"
                        fi
                        {{- end }}
                        {{- end }}
                        {{- if or .Values.postgresql.configuration .Values.postgresql.pgHba }}
                        psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c "SELECT pg_reload_conf();" > /dev/null 2>&1
                        {{- end }}
                        {{- if .Values.postgresql.lifecycle.postStart.additionalCommands }}
                        HEADLESS="{{ include "pg.fullname" . }}-headless.{{ .Release.Namespace }}.svc.cluster.local"
                        PRIMARY_HOST=""
                        for n in $(seq 0 {{ sub (int .Values.postgresql.replicaCount) 1 }}); do
                          H="{{ include "pg.fullname" . }}-${n}.${HEADLESS}"
                          if psql -h "$H" -U "$POSTGRES_USER" -d "$POSTGRES_DB" -t -c "SELECT pg_is_in_recovery();" 2>/dev/null | grep -q 'f'; then
                            PRIMARY_HOST="$H"
                            break
                          fi
                        done
                        if [ -n "$PRIMARY_HOST" ]; then
                          PGHOST="$PRIMARY_HOST" {{ .Values.postgresql.lifecycle.postStart.additionalCommands | nindent 24 }}
                        fi
                        {{- end }}
                        break
                      fi
                      sleep 1
                    done
{{- end }}
{{- end }}
          resources:
            {{- toYaml .Values.postgresql.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
{{- if .Values.postgresql.configuration }}
            - name: postgresql-config
              mountPath: /etc/postgresql/conf.d
              readOnly: true
{{- end }}
{{- if .Values.postgresql.extensions.enabled }}
            - name: ext-lib
              mountPath: /usr/lib/postgresql/18/lib
            - name: ext-share
              mountPath: /usr/share/postgresql/18/extension
{{- end }}
{{- if .Values.pgbackrest.enabled }}
            - name: pgbackrest-config
              mountPath: /etc/pgbackrest/pgbackrest.conf
              subPath: pgbackrest.conf
              readOnly: true
            - name: pg-run
              mountPath: /var/run/postgresql
{{- end }}
{{- if .Values.repmgr.enabled }}
            - name: repmgr-config
              mountPath: /etc/repmgr
        - name: repmgrd
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ["/usr/local/bin/entrypoint.sh", "repmgrd"]
          env:
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: REPMGR_USER
              value: {{ .Values.repmgr.username }}
            - name: REPMGR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretRepmgrPasswordKey" . }}
            - name: REPMGR_DB
              value: {{ .Values.repmgr.database }}
            - name: HEADLESS_SERVICE
              value: {{ include "pg.fullname" . }}-headless.{{ .Release.Namespace }}.svc.cluster.local
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - |
                    repmgr daemon stop 2>/dev/null || true
          resources:
            {{- toYaml .Values.repmgr.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: repmgr-config
              mountPath: /etc/repmgr
        - name: service-updater
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ["/bin/bash", "/scripts/service-updater.sh"]
          env:
            - name: NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: MASTER_SERVICE
              value: {{ include "pg.fullname" . }}
            - name: HEADLESS_SERVICE
              value: {{ include "pg.fullname" . }}-headless.{{ .Release.Namespace }}.svc.cluster.local
            - name: REPMGR_USER
              value: {{ .Values.repmgr.username }}
            - name: REPMGR_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretRepmgrPasswordKey" . }}
            - name: REPMGR_DB
              value: {{ .Values.repmgr.database }}
{{- if .Values.pgpool.enabled }}
            - name: PGPOOL_DEPLOYMENT
              value: {{ include "pg.fullname" . }}-pgpool
            - name: PGPOOL_SERVICE
              value: {{ include "pg.fullname" . }}-pgpool.{{ .Release.Namespace }}.svc.cluster.local
            - name: PGPOOL_PORT
              value: "{{ .Values.pgpool.service.port }}"
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretUsernameKey" . }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretPasswordKey" . }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "pg.secretName" . }}
                  key: {{ include "pg.secretDatabaseKey" . }}
{{- end }}
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/bash
                  - -c
                  - sleep 5
          volumeMounts:
            - name: service-updater-script
              mountPath: /scripts
          resources:
            {{- toYaml .Values.repmgr.serviceUpdater.resources | nindent 12 }}
{{- end }}
{{- if .Values.pgbackrest.enabled }}
        - name: pgbackrest-scheduler
          image: "{{ .Values.repmgr.image.repository }}:{{ .Values.repmgr.image.tag }}"
          imagePullPolicy: {{ .Values.repmgr.image.pullPolicy }}
          command: ["/bin/bash", "/scripts/pgbackrest-scheduler.sh"]
          env:
            - name: PGBACKREST_STANZA
              value: {{ .Values.pgbackrest.stanza }}
            - name: FULL_SCHEDULE
              value: {{ .Values.pgbackrest.schedule.full | quote }}
            - name: DIFF_SCHEDULE
              value: {{ .Values.pgbackrest.schedule.diff | quote }}
            - name: PGBACKREST_REPO1_S3_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.pgbackrest.existingSecret.name }}
                  key: {{ .Values.pgbackrest.existingSecret.accessKeyIdKey }}
            - name: PGBACKREST_REPO1_S3_KEY_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.pgbackrest.existingSecret.name }}
                  key: {{ .Values.pgbackrest.existingSecret.secretAccessKeyKey }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
            - name: pg-run
              mountPath: /var/run/postgresql
            - name: pgbackrest-config
              mountPath: /etc/pgbackrest/pgbackrest.conf
              subPath: pgbackrest.conf
              readOnly: true
            - name: pgbackrest-scripts
              mountPath: /scripts
          resources:
            {{- toYaml .Values.pgbackrest.resources | nindent 12 }}
{{- end }}
{{- if or .Values.postgresql.configuration .Values.postgresql.extensions.enabled .Values.repmgr.enabled .Values.pgbackrest.enabled (not .Values.postgresql.persistence.enabled) }}
      volumes:
{{- if .Values.postgresql.configuration }}
        - name: postgresql-config
          configMap:
            name: {{ include "pg.fullname" . }}-postgresql-config
{{- end }}
{{- if .Values.postgresql.extensions.enabled }}
        - name: ext-lib
          emptyDir: {}
        - name: ext-share
          emptyDir: {}
{{- end }}
{{- if .Values.repmgr.enabled }}
        - name: repmgr-config
          emptyDir: {}
        - name: service-updater-script
          configMap:
            name: {{ include "pg.fullname" . }}-service-updater
            defaultMode: 0755
{{- end }}
{{- if .Values.pgbackrest.enabled }}
        - name: pg-run
          emptyDir: {}
        - name: pgbackrest-config
          configMap:
            name: {{ include "pg.fullname" . }}-pgbackrest
        - name: pgbackrest-scripts
          configMap:
            name: {{ include "pg.fullname" . }}-pgbackrest-scripts
            defaultMode: 0755
{{- end }}
{{- if not .Values.postgresql.persistence.enabled }}
        - name: data
          emptyDir: {}
{{- end }}
{{- end }}
{{- if .Values.postgresql.persistence.enabled }}
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
{{- if .Values.postgresql.persistence.storageClass }}
        storageClassName: {{ .Values.postgresql.persistence.storageClass | quote }}
{{- end }}
        resources:
          requests:
            storage: {{ .Values.postgresql.persistence.size }}
{{- end }}
