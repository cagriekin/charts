# Third-party container images the pg live test-suites need, pre-pulled and warmed
# into the Kind nodes by CI (the HA image is built from source separately).
# One image ref per line; blank lines and # comments ignored. The CI base-image
# cache key is the hash of THIS file, so a chart image-tag bump no longer busts it
# (only changing this list does).
# One per PostgreSQL major in the CI matrix (#269): the standalone suites and the TLS
# suite's psql client pod run the official image, so both majors' tags must be warmed
# even though HA suites take the server from the HA image. Keep in sync
# with the mapping in pg/tests/set-pg-major.sh.
postgres:18.1-trixie
postgres:17.10-trixie
cagriekin/pgpool:4.7.1
pgpool/pgpool2_exporter:1.2.2
quay.io/prometheuscommunity/postgres-exporter:v0.19.1
quay.io/coreos/etcd:v3.5.16
busybox:1.37
busybox:1.35
# MinIO for the backup/pgbackrest suites and the chart's own mc default. quay.io, not Docker
# Hub: the minio/* repositories were pulled from Docker Hub (#348); warming them here also
# keeps the six suites that deploy MinIO from depending on a live quay pull per leg.
quay.io/minio/minio:RELEASE.2025-02-18T16-25-55Z
quay.io/minio/mc:RELEASE.2024-11-21T17-21-54Z
